let myString = "SHILPAKOORAYAIL";
console.log(myString.slice(3, 8));    // Output: "ello,"

//NOTE
// slice(start, end): Extracts a section of a string and returns 
// it as a new string. end is optional and excludes the character at that index.