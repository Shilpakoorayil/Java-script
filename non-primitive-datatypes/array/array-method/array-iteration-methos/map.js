// MAP METHOD

//Create a new array by applyling a function to each elemnt.
//Transform the array without changing the orginal.
//Return a new array with transformed values.


const double=[1,2,3].map(num=>num *2);
console.log(double);
